# OmniAvatar 智能素材中心 — 专业剪辑页 PRD

> **版本**: V2.2
> **更新日期**: 2026-03-26
> **涉及页面**: 专业剪辑页 (`/professional-edit`)、首页 (`/`)

---

## 一、页面整体布局

```
┌──────────────────────────────────────────────────────┐
│  TopBar（项目名 + 撤销/重做 + 导出）                    │
├──┬──────────────────────────────┬────────────────────┤
│图│  左侧功能面板（可拖拽调宽）     │  主画布（16:9预览）  │
│标│                              │                    │
│侧│                              │                    │
│栏│                              │                    │
├──┴──────────────────────────────┴────────────────────┤
│  时间轴（可拖拽调高）                                   │
└──────────────────────────────────────────────────────┘
```

**三区域拖拽：**
- 左面板宽度：水平拖拽，220 ~ 480 px
- 时间轴高度：垂直拖拽，160 ~ 480 px
- 拖把 hover 高亮蓝色 `#0066FF`，光标切换

---

## 二、图标侧栏

固定 64 px 宽，纵跨整个工作区（含时间轴）。

| 图标 | 标签 | AI+ |
|------|------|-----|
| `video_library` | 我的素材 | ✓ |
| `account_circle` | 虚拟人 | ✓ |
| `image` | 图片 | — |
| `spatial_audio` | 配音 | ✓ |
| `music_note` | 音乐 | — |
| `text_fields` | 文本 | — |
| `compare_arrows` | 转场 | — |
| `dashboard` | 模板 | — |
| `cloud` | 贴纸 | — |

**条件显示：** 点击轨道片段后，贴纸下方出现分隔线 + `auto_fix_high` 素材优化入口。

---

## 三、智能素材面板

点击侧栏「图片」进入，顶部 Tab 切换本地素材 / 智能素材。

### 3.1 统一生成面板（图片 & 视频合一）

图片生成和视频生成合并为一个面板，通过类型切换控制内容。

#### 提示词区

```
┌────────────────────────────────────────┐
│ [REFERENCE 上传参考图]  提示词 textarea  │
└────────────────────────────────────────┘
```

- 左侧：80×96 px 虚线参考图区域，标注「REFERENCE」
- 右侧：多行文本输入，高度自适应
- 无「示例」按钮

#### 参数控制行（单行）

```
[图片|视频] | [比例▾] [模型▾] [分辨率▾] [时长▾(仅视频)]
```

| 控件 | 图片模式 | 视频模式 |
|------|---------|---------|
| 类型切换 | 图片（选中蓝色） | 视频（选中蓝色） |
| 比例 | 16:9 / 9:16 / 4:3 / 1:1 | 同左 |
| 模型 | JIMENG 4.0 / 3.0 / 2.1 | KELING 3.0 / 2.1 / 1.6 |
| 分辨率 | 720p / 1080p / 2K | 720p / 1080p / 2K |
| 时长 | 不显示 | 4 / 5 / 6 / 8 / 10 / 12 / 15 s |

每个下拉点击弹出 Popover，点击外部关闭。

#### CTA 行

```
剩余算力          [⚡ 20]  [立即生图 / 立即生成]
48,510
```

- 左：剩余算力总额（标签 + 数字）
- 右：消耗算力徽章（深色圆角框 + 青色闪电图标 + 数字）+ 生成按钮（蓝色渐变）
- 图片消耗 20 算力，视频消耗 100 算力

#### 素材列表

- 搜索栏（左侧搜索图标，focus 蓝色描边，按 prompt 过滤）
- 筛选 Tab：**图片 | 视频**（无「全部」）
- 3 列网格，图片 1:1，视频 16:9
- 卡片 hover 显示：放大 / 加入轨道 / 下载 / 删除
- 视频卡片紫色标签「视频」，图片卡片「AI生成」

---

## 四、素材优化面板

### 4.1 触发逻辑

点击时间轴片段 → 侧栏出现「素材优化」入口 + 面板自动打开：
- AI 生成片段 → 默认「素材更换」Tab
- 本地上传片段 → 默认「动画效果」Tab

### 4.2 Tab 结构

```
[ 素材更换 ]  [ 素材编辑 ]  [ 动画效果 ]
```

### 4.3 素材更换 Tab

布局与智能素材面板完全一致：

**提示词区**
- 左侧显示当前轨道截图作为参考图（本地素材则显示空白上传框）
- 右侧文本输入

**参数控制行（单行，同智能素材）**

```
[图片|视频] | [比例▾] [模型▾] [分辨率▾] [时长▾(仅视频)]
```

- 图片模式：JIMENG 模型
- 视频模式：KELING 模型 + 时长下拉

**CTA 行（同智能素材）**

```
剩余算力          [⚡ 20]  [重新生成]
48,510
```

**素材列表（同智能素材）**
- 搜索栏 + 图片 | 视频 筛选
- 3 列网格，显示图片时额外显示「当前」高亮卡（蓝色 2px 边框）

### 4.4 素材编辑 Tab

X / Y 位置、宽度 / 高度滑杆，可重置。

### 4.5 动画效果 Tab

入场 / 出场动画各 8 种，选中蓝色高亮。

---

## 五、跨页面素材同步

通过 `smartMaterialsStore.ts` 实现首页素材中心与专业剪辑页双向同步。

| 操作 | 来源 | 效果 |
|------|------|------|
| 首页生成图片并点击「收藏至素材」 | 首页 | 自动出现在专业剪辑素材列表 |
| 首页生成视频任务点击「收藏至素材」 | 首页 | 同上 |
| 专业剪辑点击「立即生图/生成」 | 专业剪辑 | 自动出现在首页素材中心 |

Store 使用发布订阅模式，组件 `useEffect` 订阅变更。

---

## 六、时间轴

| 轨道 | 片段 | 点击行为 |
|------|------|---------|
| V1 | AI 生成片段（靛蓝色调） | 素材优化 → 素材更换 |
| V2 | 本地上传片段（绿色调）| 素材优化 → 动画效果 |

- 选中片段轮廓：亮蓝 `#60A5FA`
- 本地素材片段显示「本地」绿色徽章

---

## 七、导航

- 专业剪辑页 Header 左上角「← 返回首页」跳转至 `/`
- 首页 Header logo 右侧「movie_edit 专业剪辑」跳转至 `/professional-edit`

---

## 八、设计规范

| 用途 | 值 |
|------|-----|
| 主题蓝 | `#0066FF` |
| 选中轮廓蓝 | `#60A5FA` |
| 算力徽章闪电色 | `#22d3ee`（青色） |
| 深色背景 | `#0F0F0F` |
| 面板背景 | `#141414` |
| 分割线 | `rgba(255,255,255,0.08)` |

---

## 九、变更记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| V1.0 | 2026-03-05 | 首版基础框架 |
| V2.0 | 2026-03-25 | 智能素材面板重设计；三区域拖拽；侧栏图标体系；素材优化面板（3 子 Tab） |
| V2.1 | 2026-03-26 | 删除分镜页；返回首页按钮；侧栏 AI+ 修正；优化 popover 外部点击关闭 |
| V2.2 | 2026-03-26 | 图片/视频生成合并为统一面板（类型切换在控制行内）；视频生成新增分辨率下拉；CTA 行改为左侧显示剩余算力总额、右侧⚡消耗徽章+按钮；素材列表筛选从「全部/图片/视频」改为「图片/视频」；新增搜索栏；移除「示例」按钮和「我的智能素材」标题文字；素材更换面板布局与智能素材面板统一 |
